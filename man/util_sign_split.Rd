% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util-sign-split.R
\name{util_sign_split}
\alias{util_sign_split}
\title{Sign-split utility function}
\usage{
util_sign_split(utility_positive, utility_negative = util_leontief())
}
\arguments{
\item{utility_positive}{A \code{econ_util} object used for non-negative
quantities.}

\item{utility_negative}{A \code{econ_util} object used for (sign-flipped)
negative quantities. By default, \code{util_leontief()}.}
}
\value{
A \code{util_sign_split} object.
}
\description{
Combines a utility function for non-negative quantities
(\code{utility_positive}) with a utility function for negative quantities
(\code{utility_negative}), so that composite goods with mixed-sign quantities
(e.g. inventory drawdown, subsidies, statistical discrepancies) can be
calibrated without raising negative quantities to fractional powers.
\code{quantities} are partitioned by sign at calibration time; the negative
partition is negated before being passed to \code{utility_negative}, and the
resulting scalar is negated back.
}
