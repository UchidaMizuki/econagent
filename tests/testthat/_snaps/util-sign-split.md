# util_sign_split() gradient is not available

    Code
      util_demand_marshallian(f, c(1, 1, 1), income = 1, gradient = TRUE)
    Condition
      Error in `util_demand_marshallian()`:
      ! Gradient is not available.

